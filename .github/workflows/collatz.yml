name: Collatz Search

on:
  workflow_dispatch:
    inputs:
      limit:
        description: "Kaç sayıya kadar test edilsin?"
        required: true
        default: "100000"
      max_steps:
        description: "Bir sayı için en fazla kaç adım izlensin?"
        required: true
        default: "1000000"

jobs:
  search:
    runs-on: ubuntu-latest

    steps:
      - name: Depoyu Klonla
        uses: actions/checkout@v4

      - name: Python Ortamını Kur
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Collatz Taramasını Çalıştır
        env:
          LIMIT: ${{ inputs.limit }}
          MAX_STEPS: ${{ inputs.max_steps }}
          OUTPUT_DIR: reports
        run: python collatz.py

      - name: Raporları Artifact Olarak Yükle
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: collatz-report
          path: reports/
